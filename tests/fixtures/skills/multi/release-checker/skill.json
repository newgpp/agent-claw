{
  "name": "release-checker",
  "description": "Check release-readiness and validation steps.",
  "tools": ["read", "exec_script"],
  "scripts": ["scripts/release_check.py"],
  "tags": ["release", "validation"]
}
